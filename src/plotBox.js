import React from 'react';
import { Flex, Heading } from '@chakra-ui/react';

function PlotBox(props) {
	const { data } = props;
	return (
		<Flex>
			<Heading>Plot</Heading>
		</Flex>
	);
}

export default PlotBox;
